# Placeholder values — replace with your actual configuration
MONGO_URL=mongodb://localhost:27017
DB_NAME=portfolio

# SMTP Configuration (optional — contact form works without it, messages stored in MongoDB)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_TO=
